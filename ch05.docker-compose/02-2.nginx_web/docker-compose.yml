version: '3.8'
services:

    app:
        build: app
        volumes:
            - ./app:/code
        environment:
            FLASK_ENV: development

    nginx:
        container_name: nginx
        build: nginx
        ports:
            - 8080:80
        depends_on:
           - app
           
           