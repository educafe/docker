apiVersion: v1
kind: Pod
metadata:
  name: myutils
spec:
  containers:
  - name: myutils
    image: educafe/myubuntu:utils
    command: ["tail"]
    args: ["-f", "/dev/null"]
  terminationGracePeriodSeconds: 0


# These two pods are only for testing to access individual container in default namespace
# in terms of curl and nslookup.
