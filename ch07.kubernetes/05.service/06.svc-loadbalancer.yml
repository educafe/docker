apiVersion: v1
kind: Service
metadata:
  name: svc-loadbalancer
spec:
  type: LoadBalancer
  selector:
    run: mynginx 
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 80
      
      
      


