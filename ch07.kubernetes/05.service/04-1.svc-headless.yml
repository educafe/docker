apiVersion: v1
kind: Service
metadata:
  name: svc-headless
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    educafe: mynginx 
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 80
      
      
      


