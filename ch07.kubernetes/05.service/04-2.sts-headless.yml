apiVersion: v1
kind: Service
metadata:
  name: sts-headless
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    sts: mynginx 
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 80
      
      
      


