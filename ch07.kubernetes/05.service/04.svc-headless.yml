apiVersion: v1
kind: Service
metadata:
  name: svc-headless
spec:
  # type: ClusterIP
  clusterIP: None
  selector:
    sts: mynginx 
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
      
      
      


